program viendo;
var ints globales[1], globs[12][12];
var boos matriz[12][12];
var str strinn, stru;
var cha c;
{
  /* Este programa es
  demostracion */

  class Animal: {        /* comentario en medio de la nada */
    att: 
      var int estatura;
      var flts horario[1], comidas[2][2];

    met:
      func void cambiarEstatura() {
        print(2*2)
      }
  };

  Animal.hola > Animal.adios ? a = b : a = c;

  func int hola(ints globales[20].Animal, boo you) {
    for (i from 0 to 10 by 1) then {
      for (j from 0 to 10 by 1) then {
        read(stru)
        Animal.comidas[i][j] = stru
        Animal.estatura = 'e'       /* esto es un CHAR */
        Animal.estatura = "e"        /* esto es un STRING */
        Animal.estatura = 'ee'       /* esto es un STRING */
        var flt newOne;  
        Animal.estatura = '_'       /* esto es un CHAR */
      };
    };
    /*COMMENT */
  }
}